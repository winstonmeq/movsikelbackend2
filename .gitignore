# Node / Next.js
node_modules
.next
out
dist
build
.vercel

# Next.js generated build artifacts (rewritten on every build — never commit;
# tracking them causes pull conflicts on deploy targets like the VPS)
next-env.d.ts
tsconfig.tsbuildinfo

# Flutter
.dart_tool
.flutter-plugins
.flutter-plugins-dependencies
android/.gradle
ios/Pods
**/build

# Environment
.env
.env.local
.env.production

# Firebase service account (contains private key — never commit)
firebase-adminsdk.json
*-adminsdk-*.json

# Archives
*.zip

# Logs / system
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.DS_Store